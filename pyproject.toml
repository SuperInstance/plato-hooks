[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-hooks"
version = "0.1.0"
description = "Git hooks for real-time room events and commit triggers"
license = {text = "MIT"}
requires-python = ">=3.8"
